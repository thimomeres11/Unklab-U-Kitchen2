export interface CartItem {
  id: number;
  name: string;
  price: number;
  quantity: number;
  image: string;
}

export interface OrderForm {
  name: string;
  tableNumber: string;
  phoneNumber: string;
}

